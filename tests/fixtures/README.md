# Fixture boundary

The executable fixture lives at `src/fixtures/thai-pasir-panjang.ts`. It contains dated, partial test data derived from the 2026-09-01 retrieval probe and deliberately simplified values needed to exercise the pipeline.

They prove deterministic behavior. They are not current restaurant facts, a restaurant database, or a production fallback. The live provider never reads them.
